<%= form_for @post, html: { multipart: true } do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, placeholder: 'Title' %>
	<%= f.label :picture %>
	<%= f.file_field :picture %>
	<%= f.submit 'Post' %>
<% end %>